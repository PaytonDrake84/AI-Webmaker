import tkinter as tk
import tkinter.ttk as ttk

window = tk.Tk()

frm_Top = tk.Frame(master=window, height=20, width=300, bg="darkgrey")
btn_File = tk.Button("File")
btn_File.pack(fill=tk.X)




frm_Top.pack(fill=tk.X, side=tk.TOP)
